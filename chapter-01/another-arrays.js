// 1 -- only keep the last few

function keepTheLastFew(arr, x) {
    let result = arr.slice(x);

    return result;
}

console.log(keepTheLastFew([1, 2, 3, 4, 5, 6, 7], 4));

// 2 -- Math help ????????

// 3 -- poor kenny ??????

// 4 -- what really happened
